---
import { sections } from "@mockups/sections";
---

<ul class="links-list">
  {
    sections.map((section) => (
        <h3>{section.title}</h3> 
      <li>
        <a class="link-item" href={section.href} target="_blank" rel="noopener noreferrer">
          {section.text}
        </a>
      </li>
    ))
  }
  <span>📩 santiidele21@gmail.com</span>
</ul>
<style>
  .links-list {
    list-style: none;
    padding: 0;
    margin: 0;
    width: 100%;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    cursor: pointer;

  } 
  .link-item {
    width: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
      background: rgba(36, 55, 71, 0.377);

    border-radius: 6px;
    padding: 1rem;
    transition: .3s all ease;
    color: #e0e0e0;
  }
 
  .link-item h4 {
    margin: 0 0 0.5rem 0;
    font-size: 1.1rem;
  }
  .link-item a {
    text-decoration: none;
    font-weight: 500;
    
  }
  .link-item:hover {
 box-shadow: rgba(255, 255, 255, 0.2) 0px 2px 6px;
    background: rgba(36, 55, 71, 0.6);


  }
</style>
